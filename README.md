# benkoppe Tap

Welcome to my homebrew tap! To install a cask:

```bash
brew install --cask benkoppe/tap/<cask>
```

Or, add the tap with `brew tap benkoppe/tap` and then `brew install --cask <cask>`.

Or, in a `Brewfile`:

```ruby
tap "benkoppe/tap"
cask "<cask>"
```
